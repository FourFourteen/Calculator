#include "MainWindow.h"
#include <vector>
#include <string>

MainWindow::MainWindow() : wxFrame(nullptr, wxID_ANY, "Calculator", wxPoint(200, 200), wxSize(300, 400)) {
	
}

MainWindow::~MainWindow() {
	
}
